{
  "name": "web-tests",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint ./tests/*.test.ts",
    "dev:test": "playwright test --workers 4",
    "dev:test:codegen": "npx playwright codegen",
    "e2e:test": "start-server-and-test \"(cd ../../apps/web && npm run start)\" http://localhost:3000 \"playwright test --workers 4\""
  },
  "dependencies": {
    "start-server-and-test": "^1.14.0",
    "web": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.30.0",
    "@types/node": "^18.11.18",
    "eslint": "^7.32.0",
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*"
  }
}
